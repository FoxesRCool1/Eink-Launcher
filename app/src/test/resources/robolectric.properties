# API 36 is the newest level this Robolectric release has a full sandbox for.
# The app still compiles and ships against API 37; only the test sandbox is
# pinned. Raise it when Robolectric ships API 37 support.
sdk=36

# Roborazzi needs real drawing, not the legacy no-op canvas.
graphicsMode=NATIVE
